# Web framework
fastapi==0.115.6
uvicorn==0.32.1
python-multipart==0.0.20      # multipart form-data for the upload endpoint
pydantic==2.11.10
pydantic-settings==2.6.1
python-dotenv==1.0.0

# LLM provider clients (all three supported, pick one via LLM_PROVIDER)
google-generativeai==0.8.6    # google
anthropic==0.25.0             # claude
openai==2.31.0                # azure (Azure AI Foundry MaaS endpoints)

# Document parsing
pypdf==6.10.0                 # PDF text extraction for reality-seed uploads

# Knowledge graph (used by the legacy backend modules; pipeline uses in-memory)
networkx==3.2.1

# Optional: persistent state if you want to swap the pickle store for Postgres
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.11

# Testing
pytest==8.1.1
